# Bunker — Human To-Do

The app is fully built and functional. Here's what remains for you:

## Branding
- [ ] **Logo / wordmark** — The bunker icon is a placeholder SVG. Replace with a custom logo or wordmark that fits the brand: deliberate, calm, serious.
- [ ] **App name** — "Bunker" is the name. Confirm or pick an alternative.

## Launch
- [ ] **Domain** — Register and connect a domain (e.g. `bunker.app`, `bunker.so`, or something fitting). Update `index.html` `<title>` and any hardcoded URLs.
- [ ] **Deploy** — Run `npm run build` in `/Users/mauriello/Dev/bunker` to generate a production build in `dist/`. Deploy `dist/` to Vercel, Netlify, Cloudflare Pages, or any static host.

## Future (Phase 2+)
- [ ] **Cloud sync** — Set up a Supabase project if you want cross-device sync. Schema is defined in the product spec.
- [ ] **Browser notifications** — Design and implement the notification permission flow for deadline check-ins.
- [ ] **Mobile app** — iOS developer account needed if building a native version.

## Optional
- [ ] **Analytics** — Add a privacy-respecting analytics tool (e.g. Plausible, Fathom) if you want usage data.
- [ ] **Custom 404 page** — For the deployed version.

---

Everything else is built and working.
